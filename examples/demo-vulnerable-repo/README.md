# Intentionally vulnerable demo
Fixtures only. Values are non-functional and are included to verify redaction and detection.

The tracked `.env` file contains fake seeded credentials for the demo. Do not replace them with real secrets; production repositories should keep `.env` files ignored and store secrets in GitHub encrypted secrets or the deployment platform.
